import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
//import WhatsappHome from './components/whatsappSaver';

import Main from './index (copy)';

AppRegistry.registerComponent(appName, () => Main);
