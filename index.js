import React, {Component} from 'react';
import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';

import Main from './index (copy)'

AppRegistry.registerComponent(appName, () => Main);
